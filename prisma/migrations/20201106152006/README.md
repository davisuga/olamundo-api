# Migration `20201106152006`

This migration has been generated by Davi Suga at 11/6/2020, 12:20:06 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Progress" (
    "start_id" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "final_grade" INTEGER NOT NULL,
    "end_datetime" DATETIME NOT NULL,
    "user_id" INTEGER NOT NULL,

    FOREIGN KEY ("user_id") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    PRIMARY KEY ("start_id")
)

PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Exercise" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "title" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "url" TEXT NOT NULL,
    "answer" TEXT NOT NULL,
    "grade" INTEGER NOT NULL
);
INSERT INTO "new_Exercise" ("id", "title", "description", "url", "answer") SELECT "id", "title", "description", "url", "answer" FROM "Exercise";
DROP TABLE "Exercise";
ALTER TABLE "new_Exercise" RENAME TO "Exercise";
CREATE TABLE "new_User" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "email" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "password" TEXT NOT NULL,
    "progress" INTEGER NOT NULL DEFAULT 0
);
INSERT INTO "new_User" ("id", "email", "name", "password", "progress") SELECT "id", "email", "name", "password", "progress" FROM "User";
DROP TABLE "User";
ALTER TABLE "new_User" RENAME TO "User";
CREATE UNIQUE INDEX "User.email_unique" ON "User"("email");
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201002195846-setup-db..20201106152006
--- datamodel.dml
+++ datamodel.dml
@@ -1,9 +1,9 @@
 // This is your Prisma schema file,
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -11,9 +11,9 @@
 model User {
   id       Int     @id @default(autoincrement())
   email    String  @unique
-  name     String?
+  name     String
   password String
   progress Int     @default(0)
 }
@@ -29,5 +29,14 @@
   title       String
   description String
   url         String
   answer      String
+  grade       Int
 }
+
+model Progress {
+  start_id DateTime @id @default(now())
+  user User @relation(fields: [user_id], references: [id])
+  final_grade Int
+  end_datetime DateTime
+  user_id Int
+}
```


