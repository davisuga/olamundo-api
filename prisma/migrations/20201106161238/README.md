# Migration `20201106161238`

This migration has been generated by Davi Suga at 11/6/2020, 1:12:38 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "User" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "email" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "password" TEXT NOT NULL
)

CREATE TABLE "Lesson" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "title" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "url" TEXT NOT NULL
)

CREATE TABLE "Exercise" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "title" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "url" TEXT NOT NULL,
    "answer" TEXT NOT NULL,
    "grade" INTEGER NOT NULL
)

CREATE TABLE "Progress" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "user_id" INTEGER NOT NULL,
    "start_id" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "final_grade" INTEGER,
    "end_datetime" DATETIME,

    FOREIGN KEY ("user_id") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE UNIQUE INDEX "User.email_unique" ON "User"("email")

CREATE UNIQUE INDEX "Progress_user_id_unique" ON "Progress"("user_id")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201106155135..20201106161238
--- datamodel.dml
+++ datamodel.dml
@@ -1,9 +1,9 @@
 // This is your Prisma schema file,
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -13,10 +13,10 @@
   id       Int     @id @default(autoincrement())
   email    String  @unique
   name     String
   password String
-  progress Progress @relation(fields: [progress_start], references: [start_id])
-  progress_start DateTime @default(now())
+  progress    Progress    // relation scalar field (used in the `@relation` attribute above)
+
 }
 model Lesson {
   id          Int    @id @default(autoincrement())
@@ -34,8 +34,12 @@
   grade       Int
 }
 model Progress {
-  start_id DateTime @id @default(now())
+  id      Int     @id @default(autoincrement())
+  user    User @relation(fields: [user_id], references:[id])
+  user_id Int
+  start_id DateTime  @default(now())
   final_grade Int?
   end_datetime DateTime?
 }
+
```


